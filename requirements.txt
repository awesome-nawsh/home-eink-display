# Core display & image
Pillow

# HTTP & APIs
requests

# Environment config
python-dotenv

# MQTT (Home Assistant integration) — mqtt_client.py uses the 2.x callback
# API (CallbackAPIVersion.VERSION2), which doesn't exist in 1.x
paho-mqtt>=2.0

# Web config panel (web_config.py)
flask
werkzeug

# Secrets-at-rest encryption for password-type .env values (web_config.py)
cryptography

# System monitoring (optional, gracefully skipped if missing)
psutil

# NOTE: pigpio is a system package on Raspberry Pi OS.
# Install it with: sudo apt-get install python3-pigpio
# It is NOT available on PyPI in a usable form for Pi GPIO.
# Do not add pigpio here — install it via apt on the Pi.
